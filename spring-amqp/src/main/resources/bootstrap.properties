#required to load application properties from  spring-amqp.yml through Spring Cloud Config Server
spring.application.name=spring-amqp
# N.B. this is the default:
spring.cloud.config.uri=http://localhost:8888

management.endpoints.web.exposure.include=*